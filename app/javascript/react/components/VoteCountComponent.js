import React from "react"

const VoteCountComponent = (props) => {
    // standin, should map over the votes from props and sum the values
    let netCount = 0
    // 
    return(
        <p>{netCount}</p>
    )
}

export default VoteCountComponent